# SHROFF web console — environment
# Base URL of the ML API (FastAPI, see CONTRACTS.md). When unreachable the UI
# automatically serves contract-exact fixtures and shows "Fixture mode".
NEXT_PUBLIC_ML_API=http://localhost:8000
